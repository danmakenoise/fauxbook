var React = require( 'react' );

var ProfilePicture = React.createClass({

  render: function () {
    return <div className='profile-picture'>Profile Picture</div>;
  }
});

module.exports = ProfilePicture;
