language: ruby
rvm:
  - "2.2.0"
services:
  - postgresql
before_script:
  - npm install
  - webpack
  - psql -c 'create database fauxbook_test;' -U postgres
script:
  - RAILS_ENV=test bundle exec rake db:migrate --trace
  - bundle exec rake db:test:prepare
  - xvfb-run bundle exec rspec -l chrome
